'use client'

import { useEffect } from 'react'

export default function useValidateUser({ studentId }: { studentId: string }) {
  useEffect(() => {
    let focusTime = Date.now()
    const validateUserCheck = () => {}
    const visibilitychangeEvent = (e: Event) => {}
    const focusEvent = (e: Event) => {}
    const blurEvent = (e: Event) => {}
    return () => {}
  }, [studentId])
}
